import { escapeHtml } from '../dom.js';
export function renderAuth({ mode = 'login', error = '' } = {}) {
  const registering = mode === 'register';
  return `<section class="auth-view" aria-labelledby="auth-title"><header class="auth-heading"><p>仅限受邀用户</p><h1 id="auth-title">${registering ? '邀请码注册' : '用户登录'}</h1><span>手机号仅用于账户识别，不发送短信验证码</span></header><div class="auth-tabs" role="tablist"><button type="button" data-auth-mode="login" class="${registering ? '' : 'is-active'}">登录</button><button type="button" data-auth-mode="register" class="${registering ? 'is-active' : ''}">注册</button></div><form class="auth-form" data-auth-form="${registering ? 'register' : 'login'}"><label>中国大陆手机号<input name="phone" inputmode="numeric" autocomplete="tel" pattern="1[3-9][0-9]{9}" maxlength="11" required></label>${registering ? '<label>昵称（可选）<input name="nickname" autocomplete="nickname" maxlength="40"></label><label>邀请码<input name="inviteCode" autocomplete="one-time-code" minlength="4" maxlength="64" required></label>' : ''}<label>密码<input type="password" name="password" autocomplete="${registering ? 'new-password' : 'current-password'}" minlength="8" maxlength="64" required></label><button class="primary-action primary-action--button" type="submit">${registering ? '注册并登录' : '登录'}</button><p class="form-status" role="status">${escapeHtml(error)}</p></form><a class="text-link" href="#/privacy">查看隐私与安全说明</a></section>`;
}
