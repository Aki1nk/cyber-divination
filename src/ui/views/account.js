import { escapeHtml } from '../dom.js';
export function renderAccount(user, { error = '', success = '' } = {}) {
  return `<section class="secondary-view account-view" aria-labelledby="account-title"><header class="view-heading"><p>账户与安全</p><h1 id="account-title">${escapeHtml(user.nickname || '我的账户')}</h1><span>${escapeHtml(user.phoneMasked)}</span></header>${user.mustChangePassword ? '<article class="policy-card policy-card--boundary"><h2>必须先修改密码</h2><p>当前密码由管理员临时下发。完成修改后才能继续使用。</p></article>' : ''}<form class="settings-form auth-form" data-change-password><article><h2>修改密码</h2><label>当前密码<input type="password" name="currentPassword" autocomplete="current-password" required></label><label>新密码<input type="password" name="newPassword" autocomplete="new-password" minlength="8" maxlength="64" required></label><button class="primary-action primary-action--button" type="submit">保存新密码</button></article><p class="form-status" role="status">${escapeHtml(success || error)}</p></form><article class="policy-card"><h2>当前设备</h2><p>退出只撤销当前浏览器会话。</p><button type="button" class="secondary-action" data-user-logout>退出当前设备</button></article><form class="danger-zone" data-delete-account><h2>注销账户</h2><p>注销后所有设备立即退出，账户无法恢复。</p><label>当前密码<input type="password" name="currentPassword" required></label><label>确认文字<input name="confirmation" placeholder="输入：注销账户" required></label><button class="danger-action" type="submit">永久注销账户</button></form></section>`;
}
